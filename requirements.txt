# NETRA नेत्र — The Third Eye of Security
# Python 3.11+ required
# Install: pip3 install -r requirements.txt --break-system-packages

# ── Core framework ────────────────────────────────────────────────────────────
# anthropic>=0.40.0            # REMOVED: using Ollama instead (no API key needed)
# fastmcp>=0.4.0               # DEFERRED: MCP server for v2 release
httpx>=0.27.0                  # Async HTTP client (API calls, PoC requests, Ollama calls)
aiohttp>=3.9.0                 # Async HTTP for concurrent recon/scanning
requests>=2.31.0               # Sync HTTP for simple tool calls

# ── CLI & configuration ───────────────────────────────────────────────────────
click>=8.1.7                   # CLI framework (used internally, netra.py uses argparse)
rich>=13.7.0                   # Terminal formatting, progress bars, tables
pydantic>=2.6.0                # Data validation and settings management

# ── Report generation ─────────────────────────────────────────────────────────
python-docx>=1.1.0             # Word (.docx) report generation
reportlab>=4.1.0               # PDF report generation (compliance + main report)
openpyxl>=3.1.2                # Excel (.xlsx) 9-sheet workbook generation
jinja2>=3.1.4                  # HTML template rendering (html_report.py)

# ── Data processing ───────────────────────────────────────────────────────────
beautifulsoup4>=4.12.3         # HTML parsing for JS/endpoint extraction
lxml>=5.1.0                    # Fast XML/HTML parser (bs4 backend)
dnspython>=2.6.1               # DNS resolution for subdomain enumeration
ipaddress                      # Stdlib — IP range calculations (no install needed)

# ── Security tooling helpers ──────────────────────────────────────────────────
python-whois>=0.9.4            # WHOIS lookups for asset enrichment
shodan>=1.31.0                 # Shodan API integration (optional, needs API key)
censys>=2.2.9                  # Censys API integration (optional, needs API key)

# ── Cryptography & encoding ───────────────────────────────────────────────────
pycryptodome>=3.20.0           # Crypto primitives for evidence ZIP signing
python-jose>=3.3.0             # JWT analysis in auth_testing module

# ── Notifications ─────────────────────────────────────────────────────────────
slack-sdk>=3.27.0              # Slack notifications (netra/core/notify.py)
secure-smtplib>=0.1.1          # Secure email notifications

# ── Async / concurrency ───────────────────────────────────────────────────────
anyio>=4.3.0                   # Async backend abstraction
asyncio-throttle>=1.0.2        # Rate limiting for concurrent persona calls

# ── Storage / database ────────────────────────────────────────────────────────
# sqlite3 is stdlib — no install needed

# ── Dev / testing (optional, not required for production) ─────────────────────
pytest>=8.1.0                  # Test runner
pytest-asyncio>=0.23.0         # Async test support
pytest-cov>=5.0.0              # Coverage reporting
black>=24.3.0                  # Code formatter
ruff>=0.4.0                    # Fast linter
mypy>=1.9.0                    # Static type checker
